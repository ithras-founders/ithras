# DEPRECATED: Use the root-level cloudbuild.yaml instead.
#
# This file deployed only the backend and was missing --add-cloudsql-instances,
# which prevented Cloud SQL socket attachment on Cloud Run.
#
# The root-level cloudbuild.yaml builds and deploys both backend and frontend
# with correct Cloud SQL configuration.
#
# Run from repo root:
#   gcloud builds submit . --config=cloudbuild.yaml --substitutions=...
# See docs/CLOUD_RUN_DEPLOYMENT.md for full instructions.
